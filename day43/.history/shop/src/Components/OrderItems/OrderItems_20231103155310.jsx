import React, { useContext } from 'react'

import { AppContext } from '../../App'
export default function OrderItems({ id, quantity }) {
     const { products } = useContext(AppContext);
     return (
          <tr>
               <td>

               </td>


          </tr>
     )
}
