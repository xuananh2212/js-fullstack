const initalState = {
     todoList: []
}
export default function trelloList(state = initalState, action) {
     switch (action.type) {
          default: {
               return state;
          }
     }
}
