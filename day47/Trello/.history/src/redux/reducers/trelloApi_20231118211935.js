const initalState = {
     isLogin: false,
}
export default function trelloList(state = initalState, action) {
     switch (action.type) {
          default: {
               return state;
          }
     }
}
