const initalState = {
     tasks: []
}
export default function trelloTask(state = initalState, action) {
     switch (action.type) {

          default: {
               return state;
          }
     }
}
