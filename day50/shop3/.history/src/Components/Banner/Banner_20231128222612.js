"use client"
import { Carousel } from "antd";
import { useRef } from 'react'
export default function Banner() {
     const ref = useRef();
     return (
          <Carousel>

          </Carousel>
     )
}
